# Support

Entry points: `fixture-manifest.mjs`, `runtime-fixture-driver.mjs`, `runtime-metrics.mjs`

This directory owns shared test helpers for fixture loading, browser setup, and runtime metric extraction.

This directory does not own:
- committed baselines
- save fixture data
- one-off debug scripts
