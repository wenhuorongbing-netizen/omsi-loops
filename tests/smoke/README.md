# Smoke

Entry point: `tests/smoke/runtime-smoke.mjs`

This directory owns browser-level smoke checks for startup, localization, queue wiring, and predictor bootstrap.

This directory does not own:
- deep gameplay assertions
- balance validation
- full screenshot baselines
