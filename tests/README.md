# Tests

Entry points: `tools/run-smoke.mjs`, `tools/check-fixture-baselines.mjs`

This directory owns executable regression checks, save fixtures, and runtime baselines for refactor work.

This directory does not own:
- gameplay logic
- browser startup wiring
- ad-hoc one-off validation artifacts in `output/`
