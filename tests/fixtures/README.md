# Fixtures

Entry point: `tests/fixtures/saves/`

This directory owns reusable test inputs, especially save-state fixtures consumed by smoke and snapshot checks.

This directory does not own:
- generated screenshots
- runtime output
- fixture creation tools
